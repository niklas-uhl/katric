#!/bin/bash
#SBATCH --nodes=${nodes}
#SBATCH -o ${output_log}
#SBATCH -J ${job_name}
#SBATCH --partition=${job_queue}
#SBATCH --time=${time_string}
#SBATCH --get-user-env
#SBATCH --account=${account}
#SBATCH --ear=off

module load slurm_setup
module restore cetric

${commands}
